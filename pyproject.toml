[build-system]
requires = [
  "setuptools>=42",
  "wheel",
  "cmake",
  "ninja; platform_system!='Windows'",
  "numpy>=2.0",
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
markers = [
  "integration: marks tests that exercise integration between solver components",
]
